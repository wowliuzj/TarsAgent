{
  "name": "crypto_price",
  "version": "1.1.0",
  "description": "获取主流交易所(Binance, OKX, Coinbase, CoinGecko)的加密货币价格对并返回平均价。",
  "runtime": "python",
  "main": "src/executor.py",
  "sandbox": true,
  "parameters": {
    "type": "object",
    "properties": {
      "symbol": {
        "type": "string",
        "description": "交易对名称，如 BTCUS, BTCUSDT, BTC-USD, ETH/USDT 等。'US' 结尾会自动识别为 'USDT'。"
      }
    },
    "required": ["symbol"]
  }
}
