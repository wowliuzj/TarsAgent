#!/bin/bash

# 确保在项目根目录下运行
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
cd "$DIR"

# 使用 docker-compose 运行交互式命令
# -it 确保我们可以进行交互输入
# --rm 运行完后自动删除容器
docker-compose run --rm -it app python -m app.main "$@"
